---

name: Build

on:
  - pull_request
  - push

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
    - name: Clone
      uses: actions/checkout@v2

    - name: Build
      run: |
        docker build -t flat-manager-docker:latest .
